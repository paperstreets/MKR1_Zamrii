import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.mkr1_zamrii',
  appName: 'mkr1_zamrii',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
